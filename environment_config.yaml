attributes:

  node_name:
    value: 'elasticsearch'
    label: 'Node name'
    description: 'The name of the node where the plugin will be deployed'
    weight: 20
    type: "text"

  dedicated_disk:
    value: ''
    label: 'Dedicated disks'
    description: 'Comma-separated list of disks used to store Elasticsearch data (for instance "sdb,sdc")'
    weight: 25
    type: "text"
